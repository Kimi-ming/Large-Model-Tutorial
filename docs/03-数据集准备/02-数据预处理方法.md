# 02 - 数据预处理方法

> 📚 **学习目标**  
> - 掌握常用的数据预处理技术
> - 了解不同预处理方法的适用场景
> - 学会使用PyTorch和Transformers进行数据预处理

> 🎯 **先修要求**  
> - 完成 [01-常用数据集介绍](./01-常用数据集介绍.md)
> - 熟悉Python和PyTorch基础

> ⏱️ **预计学习时间**: 45-60分钟  
> 🏷️ **难度**: ⭐⭐⭐☆☆ 中级

---

## 📖 目录

- [为什么需要数据预处理](#为什么需要数据预处理)
- [图像预处理](#图像预处理)
- [文本预处理](#文本预处理)
- [多模态预处理](#多模态预处理)
- [预处理流水线](#预处理流水线)
- [最佳实践](#最佳实践)

---

## 为什么需要数据预处理

### 主要目的

1. **标准化输入**
   - 统一图像尺寸
   - 归一化像素值
   - 统一数据格式

2. **提升模型性能**
   - 加速收敛
   - 提高准确率
   - 增强泛化能力

3. **适配模型要求**
   - 匹配预训练模型的输入格式
   - 满足特定的数据分布

### 预处理 vs 数据增强

| 特性 | 预处理 | 数据增强 |
|------|--------|---------|
| **目的** | 标准化输入 | 扩充数据、提升泛化 |
| **时机** | 训练+推理 | 仅训练 |
| **操作** | 固定的 | 随机的 |
| **示例** | Resize, Normalize | RandomCrop, ColorJitter |

---

## 图像预处理

### 1. 尺寸调整（Resize）

**目的**: 统一图像尺寸，满足模型输入要求

**方法**:

```python
from torchvision import transforms
from PIL import Image

# 方法1: 直接缩放到目标尺寸
resize = transforms.Resize((224, 224))

# 方法2: 保持宽高比，短边缩放到目标尺寸
resize = transforms.Resize(224)

# 方法3: 保持宽高比，长边缩放到目标尺寸
resize = transforms.Resize(224, max_size=256)

# 使用
image = Image.open('dog.jpg')
resized_image = resize(image)
```

**选择建议**:

- **分类任务**: 直接缩放 `Resize((224, 224))`
- **检测任务**: 保持宽高比 `Resize(800, max_size=1333)`
- **分割任务**: 直接缩放或Pad

### 2. 中心裁剪（CenterCrop）

**目的**: 从图像中心裁剪固定尺寸

```python
# 从中心裁剪224×224区域
center_crop = transforms.CenterCrop(224)

# 常与Resize配合使用
transform = transforms.Compose([
    transforms.Resize(256),      # 短边缩放到256
    transforms.CenterCrop(224),  # 中心裁剪224×224
])
```

**适用场景**:
- ✅ 推理阶段（确定性）
- ✅ 主体居中的图像
- ❌ 训练阶段（推荐RandomCrop）

### 3. 归一化（Normalization）

**目的**: 将像素值标准化到特定分布

**原理**:
```
normalized = (image - mean) / std
```

**常用参数**:

```python
# ImageNet预训练模型的标准参数
normalize = transforms.Normalize(
    mean=[0.485, 0.456, 0.406],  # RGB三通道均值
    std=[0.229, 0.224, 0.225]    # RGB三通道标准差
)

# CLIP模型的参数
normalize = transforms.Normalize(
    mean=[0.48145466, 0.4578275, 0.40821073],
    std=[0.26862954, 0.26130258, 0.27577711]
)
```

**为什么需要归一化？**

1. **加速收敛**: 减少梯度更新的震荡
2. **数值稳定**: 避免梯度爆炸/消失
3. **匹配预训练**: 使用与预训练相同的分布

**注意事项**:
- ⚠️ 归一化前需要先转换为Tensor
- ⚠️ 使用预训练模型时，必须使用相同的归一化参数

### 4. 转换为Tensor

**目的**: 将PIL Image或numpy数组转换为PyTorch Tensor

```python
to_tensor = transforms.ToTensor()

# PIL Image (H×W×C, [0, 255]) → Tensor (C×H×W, [0.0, 1.0])
image = Image.open('dog.jpg')  # (480, 640, 3), uint8
tensor = to_tensor(image)      # (3, 480, 640), float32, [0.0, 1.0]
```

**自动完成的操作**:
1. 通道顺序: HWC → CHW
2. 数据类型: uint8 → float32
3. 值范围: [0, 255] → [0.0, 1.0]

### 5. 完整的图像预处理流水线

**训练阶段**:
```python
from torchvision import transforms

train_transform = transforms.Compose([
    transforms.Resize(256),                    # 短边缩放
    transforms.RandomCrop(224),                # 随机裁剪（数据增强）
    transforms.RandomHorizontalFlip(p=0.5),    # 随机水平翻转
    transforms.ToTensor(),                     # 转换为Tensor
    transforms.Normalize(                      # 归一化
        mean=[0.485, 0.456, 0.406],
        std=[0.229, 0.224, 0.225]
    ),
])
```

**推理阶段**:
```python
val_transform = transforms.Compose([
    transforms.Resize(256),                    # 短边缩放
    transforms.CenterCrop(224),                # 中心裁剪（确定性）
    transforms.ToTensor(),                     # 转换为Tensor
    transforms.Normalize(                      # 归一化
        mean=[0.485, 0.456, 0.406],
        std=[0.229, 0.224, 0.225]
    ),
])
```

---

## 文本预处理

### 1. 分词（Tokenization）

**目的**: 将文本转换为模型可以理解的token序列

**使用Transformers库**:

```python
from transformers import CLIPTokenizer

# 加载分词器
tokenizer = CLIPTokenizer.from_pretrained("openai/clip-vit-base-patch32")

# 分词
text = "A dog playing in the park"
tokens = tokenizer(
    text,
    padding='max_length',      # 填充到最大长度
    max_length=77,              # CLIP的最大长度
    truncation=True,            # 超长截断
    return_tensors='pt'         # 返回PyTorch Tensor
)

print(tokens['input_ids'])      # Token IDs
print(tokens['attention_mask']) # 注意力掩码
```

**输出**:
```python
{
    'input_ids': tensor([[49406,   320,  1929, ...，49407]]),  # [CLS] ... [SEP]
    'attention_mask': tensor([[1, 1, 1, ..., 0, 0, 0]])        # 1=真实token, 0=padding
}
```

### 2. 填充（Padding）

**目的**: 将不同长度的序列填充到相同长度

**策略**:

```python
# 方法1: 填充到最大长度
tokens = tokenizer(texts, padding='max_length', max_length=77)

# 方法2: 填充到批次中的最大长度（节省计算）
tokens = tokenizer(texts, padding='longest')

# 方法3: 不填充（需要手动处理）
tokens = tokenizer(texts, padding=False)
```

### 3. 截断（Truncation）

**目的**: 处理超过最大长度的文本

```python
# 启用截断
tokens = tokenizer(
    very_long_text,
    max_length=77,
    truncation=True,           # 超长部分会被截断
    truncation_strategy='longest_first'  # 截断策略
)
```

**截断策略**:
- `longest_first`: 从最长的序列开始截断
- `only_first`: 只截断第一个序列
- `only_second`: 只截断第二个序列

### 4. 特殊Token

**常见特殊Token**:

| Token | 用途 | 示例 |
|-------|------|------|
| `[CLS]` / `<|startoftext|>` | 序列开始 | BERT / CLIP |
| `[SEP]` / `<|endoftext|>` | 序列结束 | BERT / CLIP |
| `[PAD]` | 填充 | 所有模型 |
| `[MASK]` | 掩码（MLM） | BERT |
| `[UNK]` | 未知词 | 所有模型 |

**自动处理**:
```python
# Transformers会自动添加特殊token
text = "Hello world"
tokens = tokenizer(text)
# 实际处理: [CLS] Hello world [SEP]
```

---

## 多模态预处理

### 1. CLIP预处理

**图像+文本**:

```python
from transformers import CLIPProcessor
from PIL import Image

# 加载处理器（包含图像和文本预处理）
processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")

# 准备数据
image = Image.open("dog.jpg")
text = "A photo of a dog"

# 一次性处理图像和文本
inputs = processor(
    text=text,
    images=image,
    return_tensors="pt",
    padding=True
)

# 输出
print(inputs.keys())
# dict_keys(['input_ids', 'attention_mask', 'pixel_values'])
```

**批量处理**:

```python
# 批量处理多个图像和文本
images = [Image.open(f"image_{i}.jpg") for i in range(4)]
texts = ["A dog", "A cat", "A bird", "A fish"]

inputs = processor(
    text=texts,
    images=images,
    return_tensors="pt",
    padding=True
)

# 输出形状
print(inputs['pixel_values'].shape)  # (4, 3, 224, 224)
print(inputs['input_ids'].shape)     # (4, 77)
```

### 2. LLaVA预处理

**图像+对话**:

```python
from transformers import LlavaProcessor

processor = LlavaProcessor.from_pretrained("llava-hf/llava-1.5-7b-hf")

# 准备对话格式的输入
conversation = [
    {
        "role": "user",
        "content": [
            {"type": "image"},
            {"type": "text", "text": "What is in this image?"}
        ]
    }
]

# 处理
inputs = processor(
    text=processor.apply_chat_template(conversation, add_generation_prompt=True),
    images=image,
    return_tensors="pt"
)
```

---

## 预处理流水线

### 1. 使用torchvision.transforms

**优点**: 简单、高效、GPU加速

```python
from torchvision import transforms

# 定义流水线
transform = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                        std=[0.229, 0.224, 0.225]),
])

# 应用到数据集
from torchvision.datasets import ImageFolder

dataset = ImageFolder(
    root='data/train',
    transform=transform  # 自动应用到每个样本
)
```

### 2. 使用Transformers Processor

**优点**: 自动匹配模型、多模态支持

```python
from transformers import CLIPProcessor

processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")

# 自定义Dataset
class MyDataset(torch.utils.data.Dataset):
    def __init__(self, image_paths, texts, processor):
        self.image_paths = image_paths
        self.texts = texts
        self.processor = processor
    
    def __getitem__(self, idx):
        image = Image.open(self.image_paths[idx])
        text = self.texts[idx]
        
        # 使用processor处理
        inputs = self.processor(
            text=text,
            images=image,
            return_tensors="pt",
            padding='max_length',
            max_length=77
        )
        
        # 移除batch维度
        return {
            'pixel_values': inputs['pixel_values'].squeeze(0),
            'input_ids': inputs['input_ids'].squeeze(0),
            'attention_mask': inputs['attention_mask'].squeeze(0)
        }
```

### 3. 自定义预处理函数

**灵活性最高**:

```python
def custom_preprocess(image, text):
    """自定义预处理函数"""
    
    # 图像预处理
    if image.mode != 'RGB':
        image = image.convert('RGB')
    
    # 尺寸调整
    image = image.resize((224, 224), Image.BILINEAR)
    
    # 转换为numpy数组
    image = np.array(image).astype(np.float32) / 255.0
    
    # 归一化
    mean = np.array([0.485, 0.456, 0.406])
    std = np.array([0.229, 0.224, 0.225])
    image = (image - mean) / std
    
    # 转换为Tensor (HWC -> CHW)
    image = torch.from_numpy(image).permute(2, 0, 1)
    
    # 文本预处理
    # ... (使用tokenizer)
    
    return image, text_tokens
```

---

## 最佳实践

### 1. 使用与预训练相同的预处理

```python
# ❌ 错误：使用不同的归一化参数
transform = transforms.Normalize(
    mean=[0.5, 0.5, 0.5],  # 自定义参数
    std=[0.5, 0.5, 0.5]
)

# ✅ 正确：使用预训练模型的参数
from transformers import CLIPProcessor
processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")
# 自动使用正确的预处理参数
```

### 2. 训练和推理使用一致的预处理

```python
# 定义基础预处理
base_transform = [
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                        std=[0.229, 0.224, 0.225]),
]

# 训练：添加数据增强
train_transform = transforms.Compose([
    transforms.RandomResizedCrop(224),  # 额外的增强
    transforms.RandomHorizontalFlip(),
] + base_transform[2:])  # 复用后面的步骤

# 推理：只用基础预处理
val_transform = transforms.Compose(base_transform)
```

### 3. 缓存预处理结果

```python
# 对于不变的预处理，可以提前处理并缓存
class CachedDataset(torch.utils.data.Dataset):
    def __init__(self, data, transform, cache_dir='./cache'):
        self.data = data
        self.transform = transform
        self.cache_dir = cache_dir
        os.makedirs(cache_dir, exist_ok=True)
    
    def __getitem__(self, idx):
        cache_path = os.path.join(self.cache_dir, f'{idx}.pt')
        
        # 检查缓存
        if os.path.exists(cache_path):
            return torch.load(cache_path)
        
        # 处理并缓存
        item = self.transform(self.data[idx])
        torch.save(item, cache_path)
        return item
```

### 4. 批处理优化

```python
# 使用DataLoader的collate_fn进行批处理
def collate_fn(batch):
    """自定义批处理函数"""
    images = [item['image'] for item in batch]
    texts = [item['text'] for item in batch]
    
    # 批量处理（更高效）
    inputs = processor(
        text=texts,
        images=images,
        return_tensors="pt",
        padding=True
    )
    
    return inputs

# 使用
dataloader = DataLoader(
    dataset,
    batch_size=32,
    collate_fn=collate_fn  # 自定义批处理
)
```

### 5. 错误处理

```python
class RobustDataset(torch.utils.data.Dataset):
    def __getitem__(self, idx):
        try:
            image = Image.open(self.image_paths[idx])
            
            # 检查图像模式
            if image.mode != 'RGB':
                image = image.convert('RGB')
            
            # 应用预处理
            image = self.transform(image)
            
            return image, self.labels[idx]
        
        except Exception as e:
            print(f"Error loading image {idx}: {e}")
            # 返回一个默认图像
            return torch.zeros(3, 224, 224), -1
```

---

## 📊 预处理性能对比

| 方法 | 速度 | 灵活性 | GPU加速 | 推荐场景 |
|------|------|--------|---------|---------|
| **torchvision.transforms** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ | 图像分类 |
| **Transformers Processor** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | 多模态 |
| **自定义函数** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ | 特殊需求 |
| **Albumentations** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | 数据增强 |

---

## ➡️ 下一步

- [03-数据增强技术](./03-数据增强技术.md) - 学习数据增强方法
- [04-自定义数据集制作](./04-自定义数据集制作.md) - 制作自己的数据集
- [../02-模型微调技术/](../02-模型微调技术/) - 开始模型训练

---

## 📚 参考资源

- [PyTorch Transforms文档](https://pytorch.org/vision/stable/transforms.html)
- [Hugging Face Processors](https://huggingface.co/docs/transformers/main_classes/processors)
- [数据预处理最佳实践](https://pytorch.org/tutorials/beginner/data_loading_tutorial.html)

