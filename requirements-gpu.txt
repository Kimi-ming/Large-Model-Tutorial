# GPU版本依赖 - 用于NVIDIA GPU环境
# 替代 requirements.txt 用于GPU加速

# 深度学习框架（GPU版本）
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers和相关库
transformers>=4.35.0
transformers_stream_generator>=0.0.4  # Qwen-VL流式生成支持
tokenizers>=0.15.0
accelerate>=0.25.0
peft>=0.6.2

# ONNX Runtime（GPU版本）- 用于GPU加速推理
onnxruntime-gpu==1.16.3  # 支持CUDA加速

# 计算机视觉
pillow==10.1.0
opencv-python==4.8.1.78
albumentations==1.3.1

# 科学计算
numpy==1.24.3
pandas==2.1.3
scikit-learn==1.3.2
scipy==1.11.4

# ONNX和模型转换
onnx==1.15.0
onnxconverter-common==1.14.0

# API服务
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# 工具库
pyyaml==6.0.1
python-dotenv==1.0.0
loguru==0.7.2
fire==0.5.0
click==8.1.7

# 可视化
matplotlib==3.8.2
seaborn==0.13.0
plotly==5.18.0

# 进度条和日志
tqdm==4.66.1
rich==13.7.0

# HTTP客户端
requests==2.31.0
aiohttp==3.9.1
httpx==0.25.2

# HuggingFace生态
huggingface-hub==0.19.4
datasets==2.15.0
evaluate==0.4.1
safetensors==0.4.1

# 数据处理
pyarrow==14.0.1
jsonlines==4.0.0

# Jupyter支持
jupyter==1.0.0
ipywidgets==8.1.1

# TensorBoard（可选，用于训练监控）
tensorboard==2.15.1
tensorboardX==2.6.2.2

# 性能分析工具
py-spy==0.3.14
memory-profiler==0.61.0

# 测试工具（开发环境）
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1

# 代码质量工具（开发环境）
black==23.12.0
flake8==6.1.0
isort==5.13.2
mypy==1.7.1

# GPU监控工具
py3nvml==0.2.7
gpustat==1.1.1

# 注意事项：
# 1. onnxruntime-gpu 需要 CUDA 11.x 或 12.x
# 2. 确保系统已安装对应版本的 CUDA Toolkit
# 3. PyTorch GPU版本需要与CUDA版本匹配
# 4. 如果遇到CUDA版本不匹配，请访问 PyTorch官网选择对应版本：
#    https://pytorch.org/get-started/locally/

# CUDA版本对应关系：
# - CUDA 11.8: torch==2.1.0+cu118
# - CUDA 12.1: torch==2.1.0+cu121
# - 使用 pip install 时添加 --index-url https://download.pytorch.org/whl/cu118

